# Array / Searching 🔍

This folder contains basic search algorithms implemented in Java.

Files
- `BinarySearch.java` — binary search example (requires sorted input). Time: O(log n), Space: O(1).
- `Linearsearch.java` — linear search example. Time: O(n), Space: O(1).
- `SecondLargestElement.java` — (incomplete) intended to find the second largest element in an array.

How to run
- Compile: `javac BinarySearch.java`
- Run: `java BinarySearch`

Notes
- Ensure arrays are in required format (e.g., sorted for binary search).
- Add test cases and handle edge conditions (empty arrays, duplicates).