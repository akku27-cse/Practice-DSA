# 2D-Array ✅

Description
- Collection of problems and utilities that operate on 2D arrays / matrices.

Files in this folder
- `First.java` (placeholder — add problem name and description in the file header).

How to run (Java)
- Compile: `javac First.java`
- Run: `java First`

Guidelines
- Name files clearly (e.g., `MatrixRotation.java`).
- Add a short problem description and input/output format at the top of the file.
- Include time/space complexity in comments.

Contributing
- Open a PR with one problem per file and a short README update if needed.